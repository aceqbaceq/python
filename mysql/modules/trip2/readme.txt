это спец модуль исключеитльно для вызова только из под модуля trip
это сделано так как пока неумею рекурсивно запускать модуль trip.

 а нужно потому что таблица trip имеет у себя форейн кей parent_id который указвает  на эту же таблицу на
столбик trip_ip поэтому мне чтобы удалить строку из trip с заданным trip_id 
надо удалить все строки где на эту строку ссылаеются другие строки у которых parent_id 
указывает на наш trip_id  а это же полноценные строки  от которых  завчится нижележаешие таблицы
поэтому чтобы удалить строку нужно удалить другие строки от котрых завяияст нижние таблицы.
вот для удаления заививстмотей в нижних таблицых мы вызываем trip2 наделясь что степень вложеннрсти всего одна ступень.

